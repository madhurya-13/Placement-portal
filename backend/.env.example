# ---------- Database ----------
# Local dev (Docker Postgres from Milestone 1):
DATABASE_URL=postgresql+psycopg://placement_admin:placement_pass@localhost:5432/placement_portal_dev
# Production (Neon) — filled in during deployment milestone:
# DATABASE_URL=postgresql://<user>:<password>@<neon-host>/<db>?sslmode=require

# ---------- JWT Auth ----------
SECRET_KEY=changeme_generate_a_real_secret_key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ---------- Cloudinary (filled in when we build upload_service in Milestone 4) ----------
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ---------- App ----------
ENVIRONMENT=development
FRONTEND_ORIGIN=http://localhost:5173